# CabsBookingApplication

There is a dashboard showing list of types. By clicking the cab name, all the bookings and places details will be listed.
The CRUD functionality is not implemented in the Front-end, but all CRUD were implemented in API.
